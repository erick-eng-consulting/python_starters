

# se tienen precios

precio = [200, 150, 300, 50]

# solo los precios que sean mayores que 100
# utilizar el filter

resultado = filter(lambda x: x > 100, precio)
print(list(resultado))

# ciclo for